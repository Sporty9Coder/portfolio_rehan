/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', 'yourdomain.com'], // Replace with your domains
  },
};

export default nextConfig;
